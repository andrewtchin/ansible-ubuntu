server {
    listen 80;
    listen 443;
    listen [::]:80;

    server_name example.com;

    root /var/www/{{ item }};
    index index.html index.htm;

    location / {
        try_files $uri $uri/ =404;
    }
}
