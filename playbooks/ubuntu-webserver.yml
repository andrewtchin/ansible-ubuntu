---
- name: Configure Ubuntu Server as webserver.
  hosts: all
  gather_facts: yes

  roles:
    - common
    - ufw
    - user
    - authorized-keys
    - passwordless-sudo
    - nginx
    - nginx-conf-static
    - lets-encrypt
    - ssh
