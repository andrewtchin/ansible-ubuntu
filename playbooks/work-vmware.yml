---
- include: developer.yml

- name: VMware work customizations.
  hosts: all
  gather_facts: yes

  roles:
    - docker-vmware-pinned
    - docker-systemd-vmware
    - docker-compose
    - dronecli
    - gosudo
    - govc
    - delve
    - gotools
    - cfssl
    - robotframework
    - asciinema
    - packer
    - disable-idle-screen
